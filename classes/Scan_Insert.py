import thread

class Scan_Insert(object):
	
	def __init__(self, interface):
		# Add
		_interface = interafce

	def Scan(self):
		while True:
			sniff(iface = this._interface, prn = Insert)

	def Insert(self, packet):
		# Call DataBase class function to insert the packet into the right table in the database
		print "New Packet"
